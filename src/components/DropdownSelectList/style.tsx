import {StyleSheet} from 'react-native';
import colors from '../../constant/colors';

export default StyleSheet.create({
  container: {
    marginTop: 10,
    marginBottom: 20,
  },
  dropdownText: {
    fontSize: 16,
    color: '#000',
  },
  input: {
    fontSize: 16,
    color: '#000', // Default color for search text
    marginLeft: 5,
  },
});
